[project]
name = "claude-agent-sdk-tutorial"
version = "0.1.0"
description = "Hands-on Claude Agent SDK tutorial, built step by step"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "claude-agent-sdk>=0.2.144",
    "python-dotenv>=1.2.3",
]
