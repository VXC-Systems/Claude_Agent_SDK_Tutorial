# Copy this file to `.env` in this same folder, then fill in your own keys.
#
#     cp .env.example .env
#
# `.env` is git-ignored. Never commit it, and never paste a real key into a
# README, an issue, or a chat window.

# Required. Create one at https://platform.claude.com/ (paid — see the step README
# for typical per-run costs, which are small).
ANTHROPIC_API_KEY=sk-ant-api03-replace-me

# Required by the research-agent steps. Sign up at https://app.linkup.so
LINKUP_API_KEY=replace-me
